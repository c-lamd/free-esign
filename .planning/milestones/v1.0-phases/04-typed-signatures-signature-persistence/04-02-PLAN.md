---
phase: 04-typed-signatures-signature-persistence
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/lib/fonts.ts
  - src/lib/exportPdf.ts
  - src/components/PlacedFieldWidget.tsx
  - src/components/LazyPage.tsx
  - src/test/exportPdf.test.ts
  - src/test/fieldPlacement.test.ts
autonomous: true
requirements: [SIG-02, SIG-03]
user_setup: []

must_haves:
  truths:
    - "A font-backed (typed) signature/initials field exports as real embedded vector text, never a rasterized PNG"
    - "drawSignatureText sizes the typed text to fit BOTH box height and width without truncation"
    - "EXP-02 holds for a typed-signature export: the first 512 bytes are byte-identical to the input"
    - "A placed typed signature/initials renders its text on screen in the matching @font-face script font (WYSIWYG)"
    - "A typed field resizes freely (lockAspectRatio false); a drawn field still locks aspect ratio"
    - "Dropping a typed field uses armedTypedPayload and clears BOTH armedTypedPayload and armedFieldType afterward"
    - "Font bytes load same-origin via a static FONT_FILE_MAP allowlist; unknown families throw before fetch"
  artifacts:
    - path: "src/lib/fonts.ts"
      provides: "loadFontBytes(fontFamily) with FONT_FILE_MAP allowlist + module-level byte cache"
      contains: "FONT_FILE_MAP"
    - path: "src/lib/exportPdf.ts"
      provides: "registerFontkit + per-family embedFont(subset:true) + drawSignatureText + font-backed branch"
      contains: "registerFontkit"
    - path: "src/components/PlacedFieldWidget.tsx"
      provides: "font-backed render branch + dataUrl-keyed lockAspectRatio fix"
      contains: "fontFamily"
    - path: "src/components/LazyPage.tsx"
      provides: "armedTypedPayload drop branch creating font-backed fields at 200x56 default"
      contains: "armedTypedPayload"
  key_links:
    - from: "src/lib/exportPdf.ts"
      to: "src/lib/fonts.ts"
      via: "loadFontBytes import for embedFont"
      pattern: "loadFontBytes"
    - from: "src/lib/exportPdf.ts"
      to: "@pdf-lib/fontkit"
      via: "registerFontkit(fontkit) before embedFont"
      pattern: "registerFontkit\\(fontkit\\)"
    - from: "src/components/LazyPage.tsx"
      to: "src/store/fieldStore.ts"
      via: "armedTypedPayload + setArmedTypedPayload subscription"
      pattern: "armedTypedPayload"
---

<objective>
Deliver the typed-signature export slice end-to-end: a font-backed signature/initials field placed on a page renders its text on screen in the chosen script font AND exports to PDF as real embedded vector text via `@pdf-lib/fontkit` — never a rasterized PNG — with the EXP-02 zero-alteration guarantee intact.

After this plan a user can place a typed field (armed via `armedTypedPayload`, which the modal in Plan 03 will set; here it is exercised directly in tests) and download a signed PDF whose typed signature is crisp embedded text. This is the core of SIG-02/SIG-03: the typed signature is real, not an image.

Purpose: prove the anti-rasterization export path and WYSIWYG on-screen render before building the modal UI on top. This is the demoable vertical slice — type/arm → place → render → export real font.
Output: `fonts.ts` loader, extended `exportPdf.ts` (fontkit + drawSignatureText + font-backed branch), `PlacedFieldWidget.tsx` font-backed render + lockAspectRatio fix, `LazyPage.tsx` typed drop branch, and new tests.

Implements SIG-02 (typed signature embeds the chosen script font) and SIG-03 (typed initials use the same export path).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-typed-signatures-signature-persistence/04-CONTEXT.md
@.planning/phases/04-typed-signatures-signature-persistence/04-RESEARCH.md
@.planning/phases/04-typed-signatures-signature-persistence/04-PATTERNS.md
@.planning/phases/04-typed-signatures-signature-persistence/04-UI-SPEC.md
@src/lib/exportPdf.ts
@src/components/PlacedFieldWidget.tsx
@src/components/LazyPage.tsx
@src/store/fieldStore.ts
@src/test/exportPdf.test.ts
@src/test/fieldPlacement.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fonts.ts loader and extend exportPdf.ts with fontkit + drawSignatureText + font-backed branch</name>
  <read_first>
    - src/lib/exportPdf.ts (hasTextFields gate lines 134-140; drawTextInBox lines 69-85; per-field loop lines 142-179; the signature/initials image branch lines 153-172; saveIncremental→concat path lines 182-189 — DO NOT touch the concat path)
    - src/test/exportPdf.test.ts (existing EXP-02 first-512-byte test using INPUT_BYTES.buffer as ArrayBuffer; existing URL.createObjectURL stub in beforeAll — analog for the fetch mock)
    - src/store/fieldStore.ts (PlacedField with fontFamily added in Plan 01)
    - .planning/phases/04-typed-signatures-signature-persistence/04-PATTERNS.md ("src/lib/fonts.ts", "src/lib/exportPdf.ts" sections — exact excerpts incl. hasFontBackedFields gate, embeddedFonts Map, drawSignatureText)
    - .planning/phases/04-typed-signatures-signature-persistence/04-RESEARCH.md (Pattern 1 fontkit registration; Pattern 2 drawSignatureText algorithm; Pattern 3 font loading; Pitfalls 1, 3, 7)
  </read_first>
  <behavior>
    - Test: exportSignedPdf with one font-backed signature field (textValue + fontFamily, no dataUrl) does NOT throw and returns a Uint8Array longer than the input.
    - Test (EXP-02): for a font-backed signature export, output.slice(0,512) is byte-identical to input.slice(0,512) (same assertion shape as the existing drawn-signature EXP-02 test).
    - Test: a typed-initials field (type 'initials', textValue + fontFamily) exports without throwing (SIG-03 — same path).
    - Test: drawSignatureText (or the export it drives) does not truncate — a long name yields a font size smaller than the height-derived size (width constraint kicked in), i.e. the full string is drawn at a scaled-down size. Assert via a unit test on the helper if exported, or via a render-free size computation; if the helper is not exported, assert the export succeeds for a long string without error and document the no-truncation contract.
    - Test: loadFontBytes('Dancing Script') resolves via the mocked fetch; loadFontBytes('Comic Sans') (not in FONT_FILE_MAP) rejects/throws before any fetch is attempted.
  </behavior>
  <action>
    Create `src/lib/fonts.ts` (pure utility, no React): a static `const FONT_FILE_MAP: Record<string, string>` mapping exactly `'Dancing Script' → '/fonts/DancingScript-Regular.ttf'`, `'Great Vibes' → '/fonts/GreatVibes-Regular.ttf'`, `'Pacifico' → '/fonts/Pacifico-Regular.ttf'`. A module-level `const fontBytesCache = new Map<string, Uint8Array>()` (NOT React state — avoids re-renders). Export `async function loadFontBytes(fontFamily: string): Promise<Uint8Array>`: return the cached bytes if present; otherwise look up the path in FONT_FILE_MAP and THROW before fetching if the family is unknown (`Unknown font family: "${fontFamily}"`) — this is the path-traversal/PRV-02 allowlist guard (T-04-04); then `new Uint8Array(await fetch(path).then(r => r.arrayBuffer()))`, cache it, and return. Same-origin fetch only — never an external URL, never a Vite `?url` import (CONTEXT Area 5 locked).

    Extend `src/lib/exportPdf.ts`. Add imports: `fontkit from '@pdf-lib/fontkit'` and `{ loadFontBytes } from './fonts'`. After the existing `hasTextFields` gate, add a `hasFontBackedFields` gate: `fields.some(f => (f.type === 'signature' || f.type === 'initials') && !!f.textValue && !!f.fontFamily)`. When true, call `pdfDoc.registerFontkit(fontkit)` BEFORE any `embedFont` (required or FontkitNotRegisteredError is thrown — Pitfall 1). Build `const embeddedFonts = new Map<string, PDFFont>()` and, guarded by `hasFontBackedFields`, loop the fields embedding each UNIQUE `fontFamily` exactly once via `await pdfDoc.embedFont(await loadFontBytes(field.fontFamily), { subset: true })` (dedup via the Map so 5 same-font fields embed once).

    Add a `drawSignatureText(page, text, font, field)` helper (sibling of `drawTextInBox`/`drawCheckboxX`) implementing the fit-to-box algorithm WITHOUT truncation (the whole point — CONTEXT Area 2): `sizeFromHeight = font.sizeAtHeight(field.pdfHeight * 0.85)`; measure `font.widthOfTextAtSize(text, sizeFromHeight)`; `maxWidth = field.pdfWidth - 4`; `finalSize = textWidth > maxWidth ? sizeFromHeight * (maxWidth / textWidth) : sizeFromHeight`; center horizontally via xOffset and vertically via `font.heightAtSize(finalSize)`; `page.drawText(text, { x, y, font, size: finalSize })`. Do NOT call `truncateToFit`.

    In the per-field loop, inside the existing `field.type === 'signature' || field.type === 'initials'` block, branch on payload: if `field.dataUrl` is present → keep the EXISTING image path verbatim (embedPng/drawImage); else if `field.textValue && field.fontFamily` → look up `embeddedFonts.get(field.fontFamily)` and call `drawSignatureText`. Leave the date/text/checkbox branches and the `saveIncremental → concat` path completely unchanged (EXP-02 preserved).

    Extend `src/test/exportPdf.test.ts`: in `beforeAll`, alongside the existing URL stub, mock `globalThis.fetch` to resolve `{ arrayBuffer: () => Promise.resolve(<minimal-bytes>.buffer) }` so `loadFontBytes` returns bytes in jsdom (Pitfall 7) — note the EXP-02 byte-identity assertion does not depend on the TTF being parseable as long as the export path runs. If embedFont requires valid TTF bytes to not throw, instead read a small real TTF fixture from `src/test/fixtures/` and have the fetch mock return its bytes (add the fixture if needed). Add the behavior tests above.
  </action>
  <verify>
    <automated>cd /home/clamd/Projects/misc/free-esign && npx tsc -b --noEmit 2>&1 | head -20; npm test -- --run src/test/exportPdf.test.ts 2>&1 | tail -25</automated>
  </verify>
  <done>
    fonts.ts exports loadFontBytes with the FONT_FILE_MAP allowlist (throws before fetch on unknown family); exportPdf.ts registers fontkit before embedFont, embeds each unique family once with subset:true, and draws font-backed signature/initials via non-truncating drawSignatureText; the EXP-02 first-512-byte test passes for a typed-signature export; typed-initials export path verified; full exportPdf.test.ts green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add font-backed render branch + lockAspectRatio fix to PlacedFieldWidget, and typed drop branch to LazyPage</name>
  <read_first>
    - src/components/PlacedFieldWidget.tsx (checkbox computed-size branch lines 227-248 — analog; signature/initials image branch lines 174-189; shouldLockAspectRatio lines 254-255; WRAPPER_ARIA_LABEL constant lines 60-66)
    - src/components/LazyPage.tsx (handleOverlayClick lines 110-213; armed guards lines 113-115; image-aspect block lines 143-159; defaults Record lines 131-137; newField construction lines 180-193; disarm line 199; useCallback deps lines 201-213)
    - src/store/fieldStore.ts (armedTypedPayload + setArmedTypedPayload added in Plan 01)
    - .planning/phases/04-typed-signatures-signature-persistence/04-UI-SPEC.md ("PlacedFieldWidget" section — font-backed widget styles, 200x56 default, lockAspectRatio false, typed aria-labels)
    - .planning/phases/04-typed-signatures-signature-persistence/04-PATTERNS.md ("src/components/PlacedFieldWidget.tsx", "src/components/LazyPage.tsx" sections — exact excerpts)
    - .planning/phases/04-typed-signatures-signature-persistence/04-RESEARCH.md (Pattern 6 widget branch; Pitfall 2 lockAspectRatio regression; Pitfall 5 fontFamily quoting; Pitfall 6 clear armed state)
  </read_first>
  <action>
    `PlacedFieldWidget.tsx`: inside the existing `field.type === 'signature' || field.type === 'initials'` block, branch on `field.dataUrl`. If present → keep the existing `<img>` drawn branch unchanged. Else if `field.textValue && field.fontFamily` → render the font-backed branch (modeled on the checkbox computed-size branch): a centered flex `<div>` with `fontFamily: field.fontFamily` (the @font-face CSS name — same TTF as export, WYSIWYG; React resolves multi-word names in the style object, Pitfall 5), computed `fontSize = Math.min(cssHeight * 0.85, cssWidth / (field.textValue.length * 0.6 + 0.5))`, `color: var(--color-text-primary)`, `whiteSpace: 'nowrap'`, `overflow: 'hidden'`, `userSelect: 'none'`, `pointerEvents: 'none'`, full width/height. Render `field.textValue` as a text node (never dangerouslySetInnerHTML — XSS guard, React escapes; T-04-03).

    Fix `shouldLockAspectRatio` (Pitfall 2): change from keying on type alone to `((field.type === 'signature' || field.type === 'initials') && !!field.dataUrl) || field.type === 'checkbox'`. Drawn signature/initials still lock; typed (font-backed) get `lockAspectRatio={false}` and resize freely. This fix MUST land in the same task as the font-backed branch (it is broken the moment fontFamily fields exist). Update `WRAPPER_ARIA_LABEL` (or add a runtime check) so typed fields get `"Placed typed signature — press Delete to remove"` / `"Placed typed initials — press Delete to remove"` per UI-SPEC.

    `LazyPage.tsx` `handleOverlayClick`: subscribe to `armedTypedPayload` and `setArmedTypedPayload` from the store. A typed field is armed when `armedTypedPayload` is set with a matching `armedFieldType` ('signature'|'initials'); for typed, the dataUrl guards (lines 114-115) must NOT block — guard them with `!armedTypedPayload`. Skip the PNG `new Image()` aspect-ratio block (lines 143-159) for typed fields (no image to load): guard it with `&& !armedTypedPayload`. Use a typed default CSS size of `200×56` (UI-SPEC) for typed signature/initials, leaving the existing drawn defaults unchanged. In `newField`, when typed, set `{ textValue: armedTypedPayload.text, fontFamily: armedTypedPayload.fontFamily }` and NO dataUrl (evaluate this before the existing dataUrl spreads). After `addField`, clear BOTH `setArmedTypedPayload(null)` AND `setArmedFieldType(null)` (Pitfall 6 — otherwise the next click drops another typed field). Add `armedTypedPayload` and `setArmedTypedPayload` to the `useCallback` dependency array.
  </action>
  <verify>
    <automated>cd /home/clamd/Projects/misc/free-esign && npx tsc -b --noEmit 2>&1 | head -20; npm test -- --run src/test/fieldPlacement.test.ts src/test/undoRedo.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    A font-backed signature/initials field renders its textValue in fontFamily on screen; shouldLockAspectRatio is true only for drawn (dataUrl-backed) sig/initials and checkbox, false for typed; LazyPage creates a font-backed field (200×56, textValue+fontFamily, no dataUrl) when armedTypedPayload is set and clears both armed states after drop; tsc clean; fieldPlacement + undoRedo tests green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add fieldPlacement test for typed-field drop (armedTypedPayload → font-backed field + armed cleared)</name>
  <read_first>
    - src/test/fieldPlacement.test.ts (existing drop-test structure: armed via store, simulate overlay click, assert created field shape and that armedFieldType is cleared)
    - src/components/LazyPage.tsx (the typed drop branch added in Task 2)
    - .planning/phases/04-typed-signatures-signature-persistence/04-PATTERNS.md ("src/components/LazyPage.tsx" + Critical Constraints table — clear BOTH armed states)
  </read_first>
  <action>
    Add a test block to `src/test/fieldPlacement.test.ts` covering the typed drop: set `armedTypedPayload = { text: 'Jane Doe', fontFamily: 'Dancing Script', kind: 'signature' }` and `armedFieldType = 'signature'` via the store, simulate an overlay click on a page with known pageDimensions, then assert: (1) exactly one field was added with `type 'signature'`, `textValue 'Jane Doe'`, `fontFamily 'Dancing Script'`, and NO `dataUrl`; (2) the field's pdfWidth/pdfHeight derive from the 200×56 CSS default (positive, width > height); (3) after the drop, `armedTypedPayload` is `null` AND `armedFieldType` is `null` (Pitfall 6 regression guard). Add a parallel assertion for `kind: 'initials'` / `armedFieldType 'initials'` if cheap. Keep `vi.mock('idb-keyval', ...)` at module top if this file imports the store (it does transitively).
  </action>
  <verify>
    <automated>cd /home/clamd/Projects/misc/free-esign && npm test -- --run src/test/fieldPlacement.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    fieldPlacement.test.ts asserts a typed drop creates a font-backed field (textValue+fontFamily, no dataUrl, 200×56-derived size) and clears both armedTypedPayload and armedFieldType; test passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fontFamily string → fetch path | a font family name selects a static asset path for fetch |
| typed text → PDF export | user-supplied name string is drawn into the exported PDF |
| typed text → on-screen DOM | user-supplied name string is rendered in the widget |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | loadFontBytes(fontFamily) → fetch(FONT_FILE_MAP[fontFamily]) | mitigate | FONT_FILE_MAP is a static 3-key allowlist; unknown families throw BEFORE fetch — no path traversal, no arbitrary URL, same-origin only (PRV-02) |
| T-04-05 | Tampering | typed text rendered in PDF via page.drawText | mitigate | drawText emits PDF text objects, not HTML — no script execution; embedded TTF handles Unicode; empty/non-WinAnsi text guarded (drawSignatureText returns early on empty; custom TTF supports the script glyph set) |
| T-04-06 | Tampering | typed text rendered on screen in PlacedFieldWidget | mitigate | React renders field.textValue as a text node; no dangerouslySetInnerHTML — XSS-safe |
| T-04-07 | Denial of service | very long typed name in export | mitigate | drawSignatureText scales font down to fit width (no loop); input is capped at maxLength 100 by the modal in Plan 03 |
</threat_model>

<verification>
- `npx tsc -b --noEmit` clean across the slice.
- `npm test -- --run` full suite green (256 baseline + Plan 01 + new typed export/placement tests).
- EXP-02 first-512-byte identity proven for a font-backed (typed) signature export.
- Manual (end-of-phase, see human-check below): typed text renders crisp in the PDF, matching the on-screen font.
</verification>

<success_criteria>
- fonts.ts loads TTF bytes same-origin via a 3-key allowlist; unknown families throw pre-fetch.
- exportPdf.ts embeds the chosen script font (subset) once per family and draws typed signatures/initials as real text, not PNG.
- drawSignatureText fits text to box height and width with no truncation.
- EXP-02 byte-identity holds for typed exports.
- PlacedFieldWidget renders typed fields in the matching @font-face font; lockAspectRatio keys on dataUrl presence (drawn locks, typed free).
- LazyPage drops typed fields from armedTypedPayload and clears both armed states.
- Full suite green.

Human verification (end-of-phase, human_verify_mode=end-of-phase):
- In the browser, place a typed signature in each of the 3 fonts, download, open the PDF, and confirm the embedded glyphs match the on-screen preview (WYSIWYG) and are crisp at zoom (real text, not raster).
- DevTools Network tab during export shows /fonts/*.ttf loaded same-origin only — zero third-party requests.
</success_criteria>

<output>
Create `.planning/phases/04-typed-signatures-signature-persistence/04-02-SUMMARY.md` when done
</output>
