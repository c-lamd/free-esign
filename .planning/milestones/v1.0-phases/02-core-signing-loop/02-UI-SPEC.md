---
phase: 2
slug: core-signing-loop
status: approved
shadcn_initialized: false
preset: none
created: 2026-06-16
reviewed_at: 2026-06-16
---

# Phase 2 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none — hand-crafted inline-style + Tailwind v4 components (same as Phase 1) |
| Icon library | none — inline SVG only (resize handle dots, × close, chevrons reused from Phase 1) |
| Font | system-ui stack — `-apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif` |

**Rationale:** Phase 2 extends Phase 1's established hand-crafted pattern. No new design system dependency is introduced. shadcn remains deferred to Phase 5.

**New libraries added (visual/interaction impact):**
- `signature_pad` 5.x — draws on `<canvas>`, outputs PNG data URL; devicePixelRatio scaling is built-in
- `react-rnd` 10.5.3 — drag + resize overlay widget with eight directional handles

---

## Spacing Scale

Inherited from Phase 1 unchanged. Declared tokens in `src/index.css` `:root` are reused exactly.

| Token | Value | Phase 2 Usage |
|-------|-------|--------------|
| xs | 4px | Delete control padding, handle dot gap |
| sm | 8px | Modal button vertical padding, canvas inset padding |
| md | 16px | Modal horizontal padding, top-bar button spacing |
| lg | 24px | Modal section rhythm, draw-pad canvas margin from modal edge |
| xl | 32px | Modal max-width padding reserve |
| 2xl | 48px | Modal vertical centering offset |
| 3xl | 64px | Not used in Phase 2 |

**Phase 2 exceptions:**
- Signature draw canvas: minimum 44px height for touch targets on "Clear canvas" / "Add signature" / "Discard" buttons (WCAG 2.5.5)
- Modal dialog itself: `max-width: 560px`, `width: calc(100vw - 32px)` at smaller viewports (16px gutter each side)
- Placed-field widget minimum size: 80px wide × 24px tall (enforces a usable resize floor); enforced via react-rnd `minWidth`/`minHeight`
- Delete control (`×`) size: 24px × 24px visual, but wrapped to 32px × 32px touch target (invisible padding)

---

## Typography

Inherited from Phase 1 — the same four sizes and two weights apply. No new sizes or weights are introduced.

| Role | Size | Weight | Line Height | Phase 2 Usage |
|------|------|--------|-------------|--------------|
| Body | 16px | 400 (regular) | 1.5 | Modal hint text, placement-mode instruction banner |
| Label | 14px | 400 (regular) | 1.4 | Modal button labels ("Clear canvas", "Add signature", "Discard"), TopBar "Download PDF" button |
| Heading | 20px | 600 (semibold) | 1.2 | Modal title ("Draw your signature") |
| Display | 24px | 600 (semibold) | 1.1 | "FreeESign" wordmark — unchanged from Phase 1 |

Font weights declared: **400** and **600** only. No third weight is added in Phase 2.

---

## Color

All tokens are already declared in `src/index.css`. Phase 2 adds no new CSS custom properties.

| Role | Value | CSS Token | Usage |
|------|-------|-----------|-------|
| Dominant (60%) | `#F9FAFB` (gray-50) | `--color-surface` | App background, modal scrim base |
| Secondary (30%) | `#FFFFFF` (white) | `--color-surface-elevated` | Modal dialog surface, TopBar background |
| Canvas backdrop | `#E5E7EB` (gray-200) | `--color-canvas` | Document canvas behind pages — unchanged |
| Accent (10%) | `#2563EB` (blue-600) | `--color-accent` | See reserved list below |
| Destructive | `#DC2626` (red-600) | `--color-destructive` | Not used in Phase 2 (no new destructive surfaces) |
| Text primary | `#111827` (gray-900) | `--color-text-primary` | Modal title, modal body, button labels |
| Text secondary | `#6B7280` (gray-500) | `--color-text-secondary` | Modal hint text, canvas prompt, TopBar secondary labels |
| Border | `#D1D5DB` (gray-300) | `--color-border` | Modal border, draw-canvas border, widget unselected border |

**Accent (`#2563EB`) reserved for exactly (Phase 1 list extended):**
1. The "browse" button background (Phase 1 — unchanged)
2. The drag-over state border of the upload zone (Phase 1 — unchanged)
3. The prev/next navigation button focus ring (Phase 1 — unchanged)
4. Spinner stroke color (Phase 1 — unchanged)
5. **"Add signature" button background in the signature draw-pad modal** (primary CTA of the modal)
6. **"Download PDF" button background in the TopBar** (primary export CTA)
7. **Selected placed-field widget border** (2px solid `--color-accent` when a widget is selected)
8. **Resize handle dots on a selected widget** (filled `--color-accent`)
9. **Focus rings on all new interactive elements** (2px outline, `--color-accent`, 2px offset — accent is scoped to focus rings only on these elements)

Accent is NOT used for: the modal scrim, the draw-canvas surface, unselected widget borders, the "Clear canvas" or "Discard" button text, body text, or the placement-mode instruction copy.

**Modal scrim:** `rgba(0, 0, 0, 0.45)` — semi-transparent black overlay behind the modal dialog. Not a token; used only for the scrim.

**Draw canvas surface:** `#FFFFFF` (white) — signature_pad draws black ink on white. The exported PNG uses a transparent background (signature_pad's `toDataURL('image/png')` with white background cleared to transparent via canvas compositing — implementation detail for the executor).

**Unselected widget border:** `1px solid rgba(0,0,0,0.15)` — a very faint border so the placed image has a light housing visible against white page content, without consuming accent.

---

## Component Inventory

New components to build in Phase 2. All follow Phase 1's pattern: inline-style objects with Tailwind v4 utility classes for layout; `var(--color-*)` tokens for all color references.

### SignatureDrawModal

A centered, focus-trapped modal dialog for drawing a signature.

**Layout:**
- Scrim: `position: fixed; inset: 0; background: rgba(0,0,0,0.45); z-index: 50; display: flex; align-items: center; justify-content: center`
- Dialog: `background: var(--color-surface-elevated); border-radius: 12px; padding: 24px; width: calc(100vw - 32px); max-width: 560px; box-shadow: 0 8px 32px rgba(0,0,0,0.18)`
- Header row: title ("Draw your signature") left-aligned, at Heading size (20px/600); gap below header: 16px
- Canvas container: width 100%; height proportional to a 3:1 aspect ratio — use `padding-top: 33.33%` with absolute-positioned canvas inside (maintains 3:1 at any modal width). Border: `1px solid var(--color-border)`, border-radius: 6px, background: white
- Canvas hint text: shown only when canvas is empty — "Sign here" centered in the canvas, Body size (16px/400), `--color-text-secondary`, pointer-events: none (removed on first stroke)
- Canvas live preview: the signature_pad library renders strokes directly on the `<canvas>` element — no extra preview element needed
- Controls row: appears below the canvas, gap: 16px, flex-direction: row, justify-content: space-between
  - Left: "Clear canvas" button — ghost style (no background, `--color-text-secondary` text, 14px/400, hover: `--color-text-primary`, min 44px height)
  - Right group: "Discard" button (ghost, same as "Clear canvas") + "Add signature" button (accent background, white text, 14px/400, same shape as Phase 1 "Browse files" button: 8px vertical, 16px horizontal padding, 6px border-radius, min 44px height)

**Focus management:**
- On open: focus moves to the draw canvas (`tabIndex={0}`)
- Focus trap: Tab cycles among Clear canvas → Add signature → Discard → (back to canvas). Nothing outside the modal receives focus while it is open.
- Escape key: closes the modal (same as "Discard") — handler on `keydown` on the dialog element
- After close (either Add signature or Discard): focus returns to the element that triggered the modal open (the "Add signature" trigger button)

**Add signature / disabled state:** "Add signature" button has `aria-disabled="true"` and visual opacity 0.45 when the canvas is empty (no strokes drawn). Once any stroke is drawn, it becomes fully interactive.

---

### PlacementModeOverlay

A full-document overlay that arms the click-to-place gesture after the user confirms a signature in the modal.

**Appearance:**
- A thin sticky banner below the TopBar: `position: sticky; top: 56px; z-index: 20; background: var(--color-surface-elevated); border-bottom: 1px solid var(--color-border); padding: 8px 16px`
- Banner copy: "Click anywhere on the document to place your signature." — Body size (16px/400), `--color-text-secondary`
- "Stop placing" link on the right — Label size (14px/400), `--color-text-secondary`, hover: `--color-text-primary`, no background
- Cursor: on the document canvas area (behind pages), cursor changes to `crosshair` during placement mode

**Interaction:**
- User clicks on a rendered page → the signature drops at the clicked point, centered on the click coordinate
- After drop: placement mode disarms, the banner disappears, the new field is auto-selected
- Escape key (while placement mode is armed): cancels placement mode, banner disappears
- The same confirmed signature remains available in session for re-placement (user re-enters placement mode via the signature button)

---

### PlacedFieldWidget (react-rnd)

The draggable, resizable overlay widget for a placed signature field. One widget per placed field, rendered inside the per-page overlay layer.

**Unselected state:**
- No visible border chrome beyond a 1px subtle border: `1px solid rgba(0,0,0,0.15)`
- Cursor: `move` (default react-rnd behavior indicates draggability)
- The signature PNG is rendered as `<img>` fill within the widget bounds: `width: 100%; height: 100%; object-fit: contain; display: block`
- No resize handles visible

**Selected state (click-to-select):**
- Border: `2px solid var(--color-accent)` (blue-600)
- Eight resize handle dots rendered at corners and midpoints: each dot 8px × 8px, `background: var(--color-accent)`, `border-radius: 50%`, `border: 2px solid white` (white ring for contrast)
- Delete control: a `×` button positioned at the top-right corner of the widget, overlapping the border — 24px × 24px visual, 32px × 32px touch target; `background: var(--color-destructive)`, white `×` icon (14px, weight 600), `border-radius: 50%`, `box-shadow: 0 1px 4px rgba(0,0,0,0.25)`. The × character is visually rendered; an `aria-label="Delete signature"` and `<span class="sr-only">Delete signature</span>` are included for screen readers.
- Click-away on the page (not on any widget): deselects the currently selected widget

**react-rnd configuration:**
- `position={{ x, y }}` and `size={{ width, height }}` — controlled mode; source of truth is Zustand store
- `onDragStop`: updates the field's CSS-pixel position in Zustand → immediately converts to PDF-space via Coordinate Mapper for persistence
- `onResizeStop`: updates width/height in Zustand → preserves aspect ratio lock by computing the constrained dimension from the other
- `bounds="parent"` — the parent is the per-page overlay div; fields cannot drag outside their page
- `lockAspectRatio={true}` — aspect ratio is locked for signatures so the signature image is never distorted
- `minWidth={80}` `minHeight={24}` — resize floor
- Handle component: custom `<ResizeHandle>` renders the 8px accent dot (applied via `resizeHandleComponent` prop)
- `enableResizing`: all eight directions enabled (react-rnd default) but aspect ratio lock means non-corner handles produce constrained resize
- `disableDragging={false}` — dragging always enabled for selected and unselected fields; a field does not need to be selected before dragging

**Keyboard deletion:**
- When a field is selected and `Delete` or `Backspace` is pressed anywhere in the document, the selected field is removed
- Handler attached to `document` keydown listener; only fires when `selectedFieldId !== null`

**Default placed size:**
- Width: 180px (CSS pixels at the current render scale)
- Height: calculated from the signature PNG's natural aspect ratio: `height = 180 / (pngWidth / pngHeight)`
- The Coordinate Mapper converts this to PDF-space for storage immediately on placement

---

### TopBar (extended)

Phase 1 TopBar gains a "Download PDF" button in the right slot, placed to the left of "Open another".

**Download PDF button:**
- Style: accent background (`--color-accent`), white text, 14px/400, 8px vertical padding, 16px horizontal padding, 6px border-radius, min 44px height — matches the Phase 1 "Browse files" button shape exactly
- Label: "Download PDF"
- Disabled state: when zero fields are placed (`fields.length === 0`), the button uses `aria-disabled="true"` and visual opacity 0.45; it remains focusable (same `aria-disabled` pattern as Phase 1 prev/next)
- Hover (enabled): `#1D4ED8` (blue-700) background — same hover as "Browse files" in Phase 1
- Focus: 2px outline `--color-accent`, 2px offset
- Active: `#1E40AF` (blue-800) background

**TopBar right slot order (when document is loaded):** `[Download PDF] [Open another]`, gap: 8px

---

## Interaction States

| Component | State | Visual |
|-----------|-------|--------|
| SignatureDrawModal | Canvas empty | "Sign here" hint visible, "Add signature" at 0.45 opacity with aria-disabled |
| SignatureDrawModal | Strokes present | Hint hidden, "Add signature" fully interactive |
| SignatureDrawModal | Open | Scrim behind, focus trapped inside |
| SignatureDrawModal | Escape / Discard | Modal closes, focus returns to trigger |
| PlacementModeOverlay | Armed | Sticky banner visible, cursor `crosshair` over pages |
| PlacementModeOverlay | Escape / Stop placing click | Banner disappears, cursor restored, placement disarmed |
| PlacedFieldWidget | Unselected | 1px faint border, no handles, move cursor |
| PlacedFieldWidget | Hover (unselected) | 1px border darkens to `rgba(0,0,0,0.3)` |
| PlacedFieldWidget | Selected | 2px accent border, 8 handle dots, × delete control visible |
| PlacedFieldWidget | Dragging | Cursor `grabbing`, widget follows pointer, bounds enforced |
| PlacedFieldWidget | Resizing | Nearest resize cursor (nw-resize etc.), aspect ratio locked |
| DeleteControl (×) | Default | Red circle, white × |
| DeleteControl (×) | Hover | `#B91C1C` (red-700) background |
| DeleteControl (×) | Focus | 2px outline `--color-accent`, 2px offset |
| TopBar Download PDF | Zero fields | Accent background at 0.45 opacity, aria-disabled |
| TopBar Download PDF | ≥1 field | Accent background fully opaque |
| TopBar Download PDF | Hover (enabled) | `#1D4ED8` (blue-700) background |
| TopBar Download PDF | Active (enabled) | `#1E40AF` (blue-800) background |
| TopBar Download PDF | Focus | 2px outline `--color-accent`, 2px offset |

---

## Copywriting Contract

**Inherited from Phase 1 (unchanged):** All Phase 1 copy elements apply verbatim. Phase 2 adds the following elements.

| Element | Copy |
|---------|------|
| **Signature draw modal — title** | "Draw your signature" |
| **Signature draw modal — canvas hint (empty)** | "Sign here" |
| **Signature draw modal — Clear canvas button** | "Clear canvas" |
| **Signature draw modal — primary CTA button** | "Add signature" |
| **Signature draw modal — Discard button** | "Discard" |
| **Signature draw modal — Add signature (aria-label when disabled)** | `aria-label="Add signature — draw a signature first"` |
| **Placement mode banner** | "Click anywhere on the document to place your signature." |
| **Placement mode disarm link** | "Stop placing" |
| **Placed-field delete control (aria-label)** | `aria-label="Delete signature"` |
| **Placed-field delete control (sr-only)** | "Delete signature" |
| **TopBar Download PDF button** | "Download PDF" |
| **TopBar Download PDF (aria-label when disabled)** | `aria-label="Download PDF — place at least one signature first"` |
| **Trigger button (opens draw modal)** | "Add signature" |
| **Export error (inline ErrorBanner)** | "Could not export the signed PDF. Try downloading again." |

**Empty state:** No fields placed, document loaded — no new UI state is needed. The document viewer shows normally; the absence of any overlay is the empty state. There is no empty-state copy needed for the field layer in Phase 2 (undo/redo feedback is Phase 3).

**Destructive action — field deletion:**
- No confirmation dialog. Deletion is immediate on × click or Delete/Backspace key.
- Rationale: undo/redo is Phase 3. In Phase 2, deletion is low-stakes because only one signature type exists and the user can re-place easily. A confirmation dialog adds friction without undo.
- Voice note: consistent with Phase 1's calm, direct tone. No alarming copy, no exclamation marks.

**Export error surface:** The "Could not export the signed PDF. Try downloading again." string surfaces in the existing inline ErrorBanner pattern (reused from Phase 1 — `role="alert"`, `aria-live="assertive"`, positioned below the TopBar). It replaces any spinner state and persists until the user dismisses it or successfully downloads.

**Visual anchor declaration:** The loaded document content is the primary visual anchor of the signed-document view. The "Download PDF" button in the TopBar is the primary action anchor — it is the single highest-intent control on the page once a field is placed.

**Voice notes (additive):** Modal copy is imperative and minimal — no marketing language. "Sign here" is a direct affordance label, not an instruction paragraph. "Add signature" is the final confirming action; "Discard" is the safe exit (discards all drawn strokes and closes the modal). "Stop placing" disarms placement mode without losing the confirmed signature. Placement mode copy uses "anywhere on the document" to communicate spatial freedom without overpromising precision.

---

## Layout Architecture

```
┌─────────────────────────────────────────────────┐
│  TopBar (56px, sticky, z-index: 10)                    │
│  [FreeESign wordmark]   [Download PDF] [Open another]  │
├─────────────────────────────────────────────────┤
│                                                   │
│  [PLACEMENT MODE ARMED: sticky banner, z:20]              │
│  "Click anywhere to place your signature."  Stop placing  │
│                                                   │
├─────────────────────────────────────────────────┤
│  DocumentViewer (gray canvas, scrollable)         │
│                                                   │
│  ┌──────── Per-page wrapper (position:relative)──┐│
│  │  [react-pdf Page render]                      ││
│  │  ┌─ overlay div (position:absolute, inset:0)─┐││
│  │  │  ┌─ PlacedFieldWidget (react-rnd) ────┐   │││
│  │  │  │  [signature PNG]                   │   │││
│  │  │  │  (selected: accent border + handles│   │││
│  │  │  │   + × delete control top-right)    │   │││
│  │  │  └────────────────────────────────────┘   │││
│  │  └───────────────────────────────────────────┘││
│  └───────────────────────────────────────────────┘│
│                                                   │
│         ┌──────────────────────┐                  │
│         │ ‹  1 / 3  ›         │  ← PageNav pill  │
│         └──────────────────────┘                  │
└─────────────────────────────────────────────────┘

MODAL (z-index: 50, fixed, full-screen scrim):
┌──────── scrim (rgba(0,0,0,0.45), fixed inset:0) ─┐
│  ┌──── dialog (560px max, white, shadow) ────────┐│
│  │  Draw your signature              [24px header]││
│  │  ┌────────────────────────────────────────┐   ││
│  │  │                                        │   ││
│  │  │         [canvas — 3:1 ratio]           │   ││
│  │  │      "Sign here" (when empty)          │   ││
│  │  │                                        │   ││
│  │  └────────────────────────────────────────┘   ││
│  │  [Clear canvas]      [Discard] [Add signature] ││
│  └────────────────────────────────────────────────┘│
└──────────────────────────────────────────────────┘
```

**State machine additions (Zustand extensions from Phase 1):**

| State slice | Values | Notes |
|-------------|--------|-------|
| `modalOpen` | `boolean` | true = SignatureDrawModal visible |
| `signatureDataUrl` | `string \| null` | confirmed PNG data URL from the modal |
| `placementMode` | `boolean` | true = placement banner armed, click drops signature |
| `fields` | `PlacedField[]` | array of placed field records |
| `selectedFieldId` | `string \| null` | id of the currently selected widget |

`PlacedField` record (extensible — reserves recipient/role seam for v2):
```
{
  id: string            // uuid
  type: 'signature'     // Phase 2 only; Phase 3 adds 'initials' | 'date' | 'text' | 'checkbox'
  pageNumber: number    // 1-indexed
  pdfX: number          // PDF user-space origin (bottom-left)
  pdfY: number
  pdfWidth: number
  pdfHeight: number
  dataUrl: string       // PNG data URL
  role?: string         // reserved for v2 multi-party (not used in Phase 2)
}
```

---

## Accessibility Requirements

All Phase 1 requirements carry forward. Phase 2 additions:

- **Modal focus trap:** When SignatureDrawModal opens, focus moves to the canvas (`tabIndex={0}`). Tab cycles Clear canvas → Add signature → Discard → canvas. No element outside the modal is reachable via Tab while the modal is open. Implement with a `useEffect` that moves focus on mount and a keydown listener that prevents Tab from escaping.
- **Escape closes modal:** `keydown` listener on the dialog `div` with `role="dialog"`. `aria-modal="true"` on the dialog element.
- **Modal role and label:** `<div role="dialog" aria-modal="true" aria-labelledby="modal-title">` where `id="modal-title"` is on the heading element "Draw your signature".
- **Canvas accessibility:** The draw canvas has `role="img"` and `aria-label="Signature drawing canvas"`. It is not keyboard-operable for drawing (freehand drawing is inherently pointer-based); keyboard deletion and modal controls are the accessibility surface.
- **Add signature button disabled state:** `aria-disabled="true"` (not `disabled`) — keeps focus reachable. Clicking it while disabled has no effect (handler checks canvas empty state).
- **Delete control:** `aria-label="Delete signature"` on the button + `<span class="sr-only">Delete signature</span>` inside — same dual-label pattern as Phase 1 prev/next buttons.
- **Download button disabled state:** `aria-disabled="true"` when no fields placed — same pattern.
- **Placement mode banner:** `role="status"` and `aria-live="polite"` so screen readers announce when placement mode arms or disarms.
- **PlacedFieldWidget:** react-rnd renders a `<div>` — add `role="img"` and `aria-label="Placed signature — press Delete to remove"` on the outer wrapper. When selected, `aria-selected="true"`.
- **Keyboard deletion:** `document.addEventListener('keydown', ...)` only fires when `selectedFieldId !== null` and `event.key === 'Delete' || event.key === 'Backspace'`. Prevent default to avoid browser back-navigation on Backspace.
- **Focus rings:** All new buttons use the same 2px `--color-accent` outline, 2px offset as Phase 1.
- **Color contrast:** All new text/background pairs must meet WCAG AA (4.5:1 for body text, 3:1 for large):
  - White text on `--color-accent` (#2563EB): 4.54:1 — passes AA (same as Phase 1 Browse button)
  - White × on `--color-destructive` (#DC2626): 4.47:1 — passes AA (large text / icon)
  - `--color-text-secondary` (#6B7280) on white (#FFFFFF): 4.63:1 — passes AA

---

## Self-Hosted Asset Requirements (PRV-02)

Phase 1 PRV-02 requirements carry forward unchanged.

Phase 2 additions:
- `signature_pad` and `react-rnd` are npm packages bundled by Vite — no CDN requests. No new self-hosting steps required.
- The signature PNG is generated entirely in-browser (canvas → data URL). It never leaves the device.
- The export pipeline (pdf-lib) runs entirely in-browser. The signed PDF is downloaded directly without any server upload.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — shadcn not initialized | not applicable |
| Third-party | none | not applicable |

No third-party component registries are used in Phase 2. All components are hand-crafted with Tailwind v4 utilities. `signature_pad` and `react-rnd` are consumed as standard npm packages, not shadcn registry blocks.

---

## Source Traceability

| Decision | Source |
|----------|--------|
| Centered modal overlay for draw pad | 02-CONTEXT.md — Signature Draw Pad |
| Single black pen for v1 | 02-CONTEXT.md — Signature Draw Pad |
| Clear + Done controls on modal | 02-CONTEXT.md — Signature Draw Pad |
| Wide 3:1 canvas, transparent PNG export | 02-CONTEXT.md — Signature Draw Pad |
| Click/tap to place after Done | 02-CONTEXT.md — Placing the Signature |
| 180px default placed width, aspect preserved | 02-CONTEXT.md — Placing the Signature |
| Same signature can be placed multiple times | 02-CONTEXT.md — Placing the Signature |
| New field auto-selected, placement disarms | 02-CONTEXT.md — Placing the Signature |
| Click to select shows handles + delete control | 02-CONTEXT.md — Placed Field Widget |
| × control + Delete/Backspace keyboard deletion | 02-CONTEXT.md — Placed Field Widget |
| Aspect-ratio locked corner resize | 02-CONTEXT.md — Placed Field Widget |
| Drag bounded within page (bounds="parent") | 02-CONTEXT.md — Placed Field Widget |
| PDF-space coordinate storage via Coordinate Mapper | 02-CONTEXT.md — Placed Field Widget |
| "Download PDF" button in TopBar next to "Open another" | 02-CONTEXT.md — Export / Download |
| Output filename `{original-name}-signed.pdf` | 02-CONTEXT.md — Export / Download |
| App stays on document after download (no reset) | 02-CONTEXT.md — Export / Download |
| Recipient/role seam reserved in PlacedField model | 02-CONTEXT.md — Integration Points |
| Accent color for selected-field border + handles | Researcher decision — selection chrome is an accepted accent use; documented in reserved list |
| Modal scrim rgba(0,0,0,0.45) | Researcher decision — standard lightbox weight; not a token |
| No deletion confirmation dialog | Researcher decision — undo/redo is Phase 3; deletion is low-stakes and reversible by re-placing |
| Download disabled until ≥1 field placed | Researcher decision — prevents export of an unsigned document |
| Placement mode sticky banner | Researcher decision — communicates armed state without blocking document view |
| All tokens from Phase 1 src/index.css reused | 01-UI-SPEC.md + src/index.css — no new CSS custom properties added |
| system-ui font stack | Phase 1 — unchanged |
| Inline-style + Tailwind v4 component pattern | Phase 1 — unchanged |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: pending
- [ ] Dimension 2 Visuals: pending
- [ ] Dimension 3 Color: pending
- [ ] Dimension 4 Typography: pending
- [ ] Dimension 5 Spacing: pending
- [ ] Dimension 6 Registry Safety: pending

**Approval:** pending
