---
phase: 01-foundation-pdf-viewer
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-03"]
files_modified:
  - src/components/DocumentViewer.tsx
  - src/components/LazyPage.tsx
  - src/components/PageNavigation.tsx
  - src/store/documentStore.ts
  - src/test/documentViewer.test.ts
autonomous: false
requirements: [DOC-03]
user_setup: []

must_haves:
  truths:
    - "A multi-page PDF renders as a continuous vertical scroll of centered pages on a gray canvas, each at fit-to-width"
    - "A user can move forward and backward through all pages via prev/next buttons and via scrolling"
    - "The page indicator shows '1 / N' and updates as the user scrolls or navigates"
    - "Off-screen pages are lazy-rendered (not all rendered at once), so a long PDF does not OOM the tab"
  artifacts:
    - path: "src/components/DocumentViewer.tsx"
      provides: "react-pdf <Document> + fit-to-width container (ResizeObserver) + lazy page list, replacing the Plan-01 skeleton"
      contains: "ResizeObserver"
    - path: "src/components/LazyPage.tsx"
      provides: "IntersectionObserver wrapper that renders a placeholder until the page enters the viewport"
      contains: "IntersectionObserver"
    - path: "src/components/PageNavigation.tsx"
      provides: "Bottom-center pill: prev/next (44px, aria-labels + sr-only) + '1 / N' indicator (aria-live)"
      contains: "aria-live"
  key_links:
    - from: "src/components/DocumentViewer.tsx"
      to: "documentStore (numPages, currentPage)"
      via: "setNumPages on Document onLoadSuccess; setCurrentPage from scroll/IntersectionObserver"
      pattern: "(setNumPages|setCurrentPage)"
    - from: "src/components/PageNavigation.tsx"
      to: "DocumentViewer scroll container"
      via: "prev/next scrolls to the target page; indicator reads currentPage / numPages"
      pattern: "scrollTo|scrollIntoView"
---

<objective>
Replace the Plan-01 skeleton viewer with the full continuous, lazy, multi-page document viewer: pages render as a vertical scroll of centered, fit-to-width pages on a neutral gray canvas, off-screen pages are lazy-rendered via IntersectionObserver, and a bottom-center navigation pill (prev/next + "1 / N") plus scrolling lets the user move through every page. This completes DOC-03 and the Phase 1 user story.

Purpose: Make "view and navigate all pages" real and performant for documents of any length, honoring every locked CONTEXT.md layout decision (continuous scroll, fit-to-width, centered pages, gray canvas, page indicator + prev/next).
Output: DocumentViewer (full), LazyPage, PageNavigation, store wiring for currentPage tracking, and a component test asserting numPages drives the page-list length.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@CLAUDE.md
@.planning/phases/01-foundation-pdf-viewer/01-RESEARCH.md
@.planning/phases/01-foundation-pdf-viewer/01-UI-SPEC.md

<interfaces>
<!-- Consumed from Plan 01 (already built). Use directly. -->
From src/store/documentStore.ts:
```typescript
useDocumentStore: {
  docUrl: string | null
  numPages: number | null
  currentPage: number          // 1-indexed
  setNumPages: (n: number) => void   // also sets view='loaded'
  setCurrentPage: (n: number) => void
}
```
From src/lib/pdfWorker.ts:
```typescript
export const pdfOptions   // { cMapUrl:'/cmaps/', cMapPacked:true, standardFontDataUrl:'/standard_fonts/' }
// import pdfWorker module at the TOP of DocumentViewer.tsx before react-pdf imports (Plan 01 already did this in the skeleton; preserve it).
```
</interfaces>

<dependency_note>
Wave 3: depends on Plan 01 (store, pdfWorker, skeleton DocumentViewer it replaces) and Plan 03 (the upload flow is the entry into 'loaded'; this plan is verified by opening a real multi-page document through that flow). Modifies DocumentViewer.tsx (Plan 01) and documentStore.ts — both already settled by the time this wave runs.
</dependency_note>
</context>

## Phase Goal

**As a** person who needs to sign a document, **I want to** open a PDF or image and see every page rendered in my browser, **so that** I can review it knowing the file never leaves my device.

(This plan completes the goal: every page of a multi-page document renders and is navigable.)

<tasks>

<task type="auto">
  <name>Task 1: LazyPage + fit-to-width DocumentViewer (continuous multi-page render)</name>
  <files>src/components/LazyPage.tsx, src/components/DocumentViewer.tsx, src/test/documentViewer.test.ts</files>
  <read_first>
    - 01-RESEARCH.md "Pattern 2: Fit-to-Width Pages with ResizeObserver", "Pattern 3: Lazy Page Rendering via IntersectionObserver", "Verified: react-pdf Document + Page", Pitfall 5 (ResizeObserver infinite loop — give the container a constrained width, never width:fit-content)
    - 01-UI-SPEC.md "DocumentViewer" (gray canvas, centered pages max-width, white page bg + shadow 0 2px 8px rgba(0,0,0,0.12), 24px page gap, 24px vertical padding)
    - The <interfaces> block (store + pdfOptions) — preserve the Plan-01 pdfWorker import order
  </read_first>
  <action>
    Create src/components/LazyPage.tsx per RESEARCH Pattern 3: a wrapper div observed by IntersectionObserver (rootMargin '200px'); while off-screen it renders a placeholder div with an estimated min-height (≈ containerWidth × 1.414) to preserve scroll height; once intersecting, it renders react-pdf `<Page pageNumber={n} width={containerWidth} renderTextLayer={false} renderAnnotationLayer={false} />`. Expose a ref/data attribute (e.g. `data-page-number`) so PageNavigation can scroll to it. Style each page white with the UI-SPEC shadow.

    Replace src/components/DocumentViewer.tsx (the Plan-01 skeleton) with the full viewer: keep the pdfWorker import at the very top (before react-pdf), wrap a scrollable container (gray `--color-canvas` bg, 24px vertical padding) measured by a ResizeObserver via a useCallback ref to track containerWidth (Pattern 2); give the container a constrained width (max-width / 100% inside a fixed parent — never fit-content, Pitfall 5). Render react-pdf `<Document file={docUrl} options={pdfOptions} onLoadSuccess={({numPages}) => setNumPages(numPages)} onLoadError={...} loading={<LoadingSpinner/>}>` then map `1..numPages` to `<LazyPage pageNumber={n} containerWidth={cw} />`, centered, 24px gap. Render <PageNavigation/> (built in Task 2) fixed bottom-center.

    Write src/test/documentViewer.test.ts (jsdom + @testing-library/react): mock react-pdf's Document to invoke onLoadSuccess with a known numPages (e.g. 5) and assert the rendered LazyPage count (or page placeholders) equals numPages — covering the VALIDATION map's DOC-03 unit/component test. Mock IntersectionObserver/ResizeObserver in the test (jsdom lacks them).
  </action>
  <verify>
    <automated>npx vitest run src/test/documentViewer.test.ts 2>&1 | tail -10 && npx tsc --noEmit 2>&1 | tail -5 && echo VIEWER_OK</automated>
  </verify>
  <acceptance_criteria>
    - DocumentViewer renders a <Document> with `options={pdfOptions}` and maps numPages → that many LazyPage entries on a gray canvas, centered, fit-to-width.
    - LazyPage uses IntersectionObserver and renders a placeholder (preserving scroll height) until visible; pages use renderTextLayer/renderAnnotationLayer false.
    - documentViewer.test.ts proves numPages from onLoadSuccess drives the page-list length and is GREEN.
    - tsc --noEmit passes; no width:fit-content on the viewer container.
  </acceptance_criteria>
  <done>A multi-page PDF renders as a continuous, lazy, fit-to-width vertical scroll of centered pages.</done>
</task>

<task type="auto">
  <name>Task 2: PageNavigation pill + currentPage tracking</name>
  <files>src/components/PageNavigation.tsx, src/store/documentStore.ts, src/components/DocumentViewer.tsx</files>
  <read_first>
    - 01-UI-SPEC.md "PageNavigation" (fixed bottom-center, white pill 20px radius, shadow, 8px×16px padding; prev/next 16px chevron SVG, 44px touch target, disabled at boundaries opacity 0.35; BOTH aria-label AND inner sr-only span; indicator "1 / N" Label 14px/400), "Interaction States" (prev/next hover/disabled/focus), "Accessibility Requirements" (aria-live=polite on the indicator; aria-disabled not disabled at boundaries; sr-only defined in index.css by Plan 01)
    - The <interfaces> block (currentPage/setCurrentPage/numPages)
  </read_first>
  <action>
    Build src/components/PageNavigation.tsx per UI-SPEC: a fixed bottom-center white pill (20px radius, the specified shadow, 8px/16px padding) containing [prev] [indicator] [next], 8px gap. Prev/next are icon-only inline-SVG chevrons with a 44px minimum touch target, `--color-text-primary` default / accent on hover / 2px accent focus outline; at the first page disable prev and at the last page disable next using `aria-disabled="true"` (NOT the `disabled` attribute, so focus stays reachable) and opacity 0.35. Each button carries BOTH an `aria-label` ("Previous page"/"Next page") AND an inner `<span class="sr-only">` label. The indicator renders `{currentPage} / {numPages}` with `aria-live="polite"`.

    Wire navigation to the DocumentViewer scroll container: prev/next compute `currentPage ∓ 1` (clamped to 1..numPages), `setCurrentPage`, and scroll that page into view (e.g. find the LazyPage by `data-page-number` and `scrollIntoView`). Track currentPage from scrolling: in DocumentViewer use an IntersectionObserver (or scroll-position math) over the LazyPage wrappers to `setCurrentPage(n)` for the most-visible page, so the indicator stays in sync with manual scrolling. Keep the store's currentPage as the single source of truth (extend documentStore.ts only if a helper is genuinely needed; do not break the Plan-01 contract).
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -6 && npx tsc --noEmit 2>&1 | tail -5 && echo NAV_OK</automated>
  </verify>
  <acceptance_criteria>
    - PageNavigation is a fixed bottom-center pill with prev/next (44px, aria-label + sr-only span, aria-disabled at boundaries) and a "1 / N" indicator with aria-live=polite.
    - Prev/next change currentPage (clamped) and scroll the target page into view; scrolling updates currentPage so the indicator follows.
    - Full `npx vitest run` is green; tsc --noEmit passes.
  </acceptance_criteria>
  <done>Users can navigate every page via prev/next and scrolling, with an accurate, accessible page indicator.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — multi-page navigation, image-as-document, and the full Phase 1 flow</name>
  <action>Pause for the user to exercise the full Phase 1 flow in a browser — multi-page navigation, lazy rendering on a long PDF, image-as-document, and the zero-third-party-request audit (DOC-03 + PRV-02 success criteria) — none of which is unit-testable. Do not proceed until the user approves.</action>
  <what-built>The complete Phase 1 viewer: continuous fit-to-width multi-page rendering on a gray canvas, lazy page loading, a bottom-center prev/next + "1 / N" navigation pill, and the full open-document flow (PDF + image) from Plans 01/03.</what-built>
  <how-to-verify>
    1. Run `npm run dev`. Open a MULTI-PAGE PDF (drag-drop and via Browse).
    2. Confirm pages render as a continuous vertical scroll, centered, fit-to-width, on a gray canvas, with a subtle shadow and gaps between pages.
    3. Use prev/next and scroll through ALL pages. Confirm the "1 / N" indicator updates both when clicking prev/next and when scrolling; confirm prev is disabled on page 1 and next on the last page.
    4. Scroll a long PDF quickly — confirm pages render lazily (placeholders before render) without the tab hanging/OOMing.
    5. Open a JPG and a PNG — confirm each displays as a single-page document (image wrapped to PDF).
    6. Re-open DevTools → Network and confirm the multi-page load still produces zero third-party-origin requests.
    7. Click "Open another" and confirm you return to the upload empty state without a page reload, then open a different document.
  </how-to-verify>
  <resume-signal>Type "approved" if multi-page navigation, lazy rendering, image-as-document, and zero third-party requests all work, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → network | Multi-page render must not trigger any third-party request (CMaps/fonts/worker stay local). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | DoS | rendering all pages of a large PDF | mitigate | LazyPage (IntersectionObserver) renders only on-screen pages with placeholders for the rest (RESEARCH Pattern 3 / anti-pattern: never render all pages). |
| T-01-10 | Information Disclosure | CMap/font fetch on multi-page render | mitigate | `options={pdfOptions}` (local cMapUrl/standardFontDataUrl) on the multi-page <Document>; re-verified in the Task 3 Network audit (PRV-02). |
| T-01-03 | Tampering | annotation-layer injection | mitigate | `renderAnnotationLayer={false}` on every LazyPage. |
| T-01-SC | Tampering | npm installs | n/a | No new packages installed. |
</threat_model>

<verification>
- `npx vitest run` green (adds the DocumentViewer component test).
- `npx tsc --noEmit` passes.
- Task 3 manual check: multi-page navigation works, lazy rendering holds on long PDFs, image-as-document works, and the multi-page load shows zero third-party requests.
</verification>

<success_criteria>
- DOC-03: a user can navigate forward and backward through all pages of a multi-page PDF (success criterion 3).
- Pages render continuous, centered, fit-to-width on a gray canvas per the locked CONTEXT.md layout decisions.
- Off-screen pages are lazy-rendered; long PDFs do not OOM the tab.
- The Phase 1 user story is fully satisfied: open any PDF or image and view every page, with nothing leaving the browser.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-pdf-viewer/01-04-SUMMARY.md` when done.
</output>
