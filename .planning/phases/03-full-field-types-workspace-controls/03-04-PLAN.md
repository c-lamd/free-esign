---
phase: 03-full-field-types-workspace-controls
plan: 04
type: execute
wave: 4
depends_on: ["03-02", "03-03"]
files_modified:
  - src/components/UndoRedoControls.tsx
  - src/components/InitialsDrawModal.tsx
  - src/components/DocumentViewer.tsx
  - src/components/TopBar.tsx
  - src/App.tsx
  - src/test/undoRedo.test.ts
autonomous: true
requirements: [FLD-02, FLD-09]
must_haves:
  truths:
    - "The TopBar shows Undo and Redo buttons, leftmost in the loaded toolbar group"
    - "Undo is disabled when there is nothing to undo; Redo is disabled when there is nothing to redo"
    - "Cmd/Ctrl+Z undoes, Cmd/Ctrl+Shift+Z and Ctrl+Y redo placement/edit actions"
    - "Undo/redo keyboard shortcuts do NOT fire while focus is inside a text/date field input"
    - "The user can draw initials in a focus-trapped modal ('Draw your initials') and, on confirm, arm initials placement"
    - "A placed initials field renders as a transparent-PNG image with a smaller default footprint than a signature"
    - "A new action after an undo truncates the redo tail (FLD-09)"
  artifacts:
    - path: "src/components/UndoRedoControls.tsx"
      provides: "Undo/Redo ghost buttons + keyboard handler with input guard, canUndo/canRedo from history"
      contains: "historyIndex"
    - path: "src/components/InitialsDrawModal.tsx"
      provides: "Focus-trapped initials draw modal mirroring SignatureDrawModal; sets initialsDataUrl + arms 'initials'"
      contains: "Draw your initials"
  key_links:
    - from: "src/components/UndoRedoControls.tsx"
      to: "fieldStore.undo / redo"
      via: "buttons + keydown handler guarded against INPUT/TEXTAREA"
      pattern: "undo"
    - from: "src/components/InitialsDrawModal.tsx"
      to: "fieldStore.setInitialsDataUrl + setArmedFieldType"
      via: "on confirm: store PNG then arm 'initials'"
      pattern: "setArmedFieldType"
---

<objective>
Add the undo/redo workspace controls (toolbar buttons + keyboard shortcuts) and the initials draw-and-place capability — closing FLD-09's UI surface and completing FLD-02's initials type.

This plan finishes two vertical slices that depend on the wired-up history (Plan 02 already pushes history on add/delete/drag/resize/blur) and the placement drop dispatch (Plan 02's LazyPage already handles `armedFieldType === 'initials'` given an `initialsDataUrl`). Here we add: (1) the Undo/Redo buttons + global keyboard shortcuts with the input-focus guard, so a user can step backward/forward through placement and edits; and (2) the InitialsDrawModal — a near-identical sibling of SignatureDrawModal that produces a transparent-PNG initials image and arms 'initials' placement, which then drops a smaller image field via the existing Plan 02 path.

Per CONTEXT.md Area 1 (initials = draw modal, smaller footprint) and Area 3 (undo/redo controls + shortcuts + input guard), the UI-SPEC UndoRedoControls/InitialsDrawModal contracts, and RESEARCH Sections 4 (keyboard guard) and the InitialsDrawModal PATTERNS analog.

Purpose: Undo/redo makes the workspace forgiving (deletion fully recoverable); initials are a core annotation type.
Output: New `UndoRedoControls.tsx` and `InitialsDrawModal.tsx`; `TopBar.tsx` hosts UndoRedoControls; `DocumentViewer.tsx` owns the undo/redo keydown handler; `App.tsx` mounts the InitialsDrawModal; new `undoRedo.test.ts` consolidates FLD-09 + initials-history coverage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-full-field-types-workspace-controls/03-CONTEXT.md
@.planning/phases/03-full-field-types-workspace-controls/03-RESEARCH.md
@.planning/phases/03-full-field-types-workspace-controls/03-PATTERNS.md
@.planning/phases/03-full-field-types-workspace-controls/03-UI-SPEC.md
@.planning/phases/03-full-field-types-workspace-controls/03-02-SUMMARY.md
@.planning/phases/03-full-field-types-workspace-controls/03-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- `UndoRedoControls` — new component at `src/components/UndoRedoControls.tsx` (2 ghost buttons + global keydown handler)
- `InitialsDrawModal` — new component at `src/components/InitialsDrawModal.tsx` (focus-trapped, 2:1 canvas, "Add initials")
- `canUndo = historyIndex > 0`, `canRedo = historyIndex < history.length - 1` derivations
- `src/test/undoRedo.test.ts` — new store-level test file for FLD-09 redo-tail truncation + initials-history integration
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: UndoRedoControls — buttons + guarded keyboard shortcuts</name>
  <files>src/components/UndoRedoControls.tsx, src/components/TopBar.tsx, src/components/DocumentViewer.tsx</files>
  <read_first>
    - src/components/DocumentViewer.tsx (the file being modified — the EXISTING Delete/Backspace keydown handler lines 61-81 with the INPUT/TEXTAREA/contentEditable guard at 67-72; this is the guard to reuse/extend)
    - src/components/TopBar.tsx (Plan 02 output — loaded button group now hosting FieldPalette; UndoRedoControls prepends to the LEFT of FieldPalette per UI-SPEC order)
    - src/components/PageNavigation.tsx (ghost button + aria-disabled + sr-only + focus-ring analog)
    - .planning/phases/03-full-field-types-workspace-controls/03-PATTERNS.md (UndoRedoControls section: ghost button style, keyboard handler copy+extend from DocumentViewer, store wiring canUndo/canRedo)
    - .planning/phases/03-full-field-types-workspace-controls/03-RESEARCH.md (Section 4: keyboard shortcut guard, "extend that handler rather than adding a second addEventListener"; Pitfall 8)
    - .planning/phases/03-full-field-types-workspace-controls/03-UI-SPEC.md (UndoRedoControls lines 144-159, TopBar order line 158-159; Copywriting Contract undo/redo labels lines 346-351; Color line 102; Interaction States 288-294)
  </read_first>
  <action>
    Create `UndoRedoControls.tsx` per the UI-SPEC + PATTERNS.md. Two ghost buttons (Undo, then Redo), each 44×44 min touch target, ghost style (`--color-text-secondary` icon, hover → `--color-text-primary`), 16px inline SVG arrows (Undo = counterclockwise, Redo = clockwise, aria-hidden), `sr-only` span, and the 2px accent focus-ring. Subscribe to `undo`, `redo`, `historyIndex`, and `history.length` from fieldStore; derive `canUndo = historyIndex > 0` and `canRedo = historyIndex < history.length - 1`. When `!canUndo`/`!canRedo`: `aria-disabled="true"`, opacity 0.35, default cursor, and the disabled-state aria-label ("Undo — nothing to undo" / "Redo — nothing to redo"); enabled labels "Undo"/"Redo". Clicking calls `undo()`/`redo()`.

    Keyboard shortcuts: per RESEARCH Section 4, EXTEND the EXISTING keydown handler in `DocumentViewer.tsx` rather than adding a second `document.addEventListener` (avoids double-binding). In that handler, AFTER the existing INPUT/TEXTAREA/contentEditable guard (which already returns early — so undo/redo will NOT fire while typing, satisfying Pitfall 8 and the accessibility requirement), add: `isMod = e.metaKey || e.ctrlKey`; if `isMod && shiftKey && key==='z'/'Z'` → preventDefault + redo; else if `isMod && key==='z'/'Z'` → preventDefault + undo; else if `e.ctrlKey && key==='y'/'Y'` → preventDefault + redo. Subscribe DocumentViewer to `undo`/`redo` from fieldStore and add them to the existing handler's effect dependency array (alongside the existing selectedFieldId/deleteField deps). Keep the Delete/Backspace logic intact. Because the guard returns before any shortcut branch, shortcuts are inert while a text/date input is focused. NOTE: the Delete/Backspace branch returns early when `!selectedFieldId`; ensure the undo/redo branches are reached regardless of selection (place them so an early `return` on no-selection does not skip undo/redo — restructure the handler so the Delete/Backspace selection check does not block the undo/redo keys).

    In `TopBar.tsx`: render `<UndoRedoControls />` as the FIRST item in the `view === 'loaded'` group (left of FieldPalette), matching the UI-SPEC order `[Undo][Redo] | [palette] | [Download][Open another]`. Add a thin 1px `--color-border` vertical separator (height 20px, margin 0 4px) between UndoRedoControls and FieldPalette if not already present.
  </action>
  <verify>
    <automated>cd /home/clamd/Projects/misc/free-esign && npx tsc -b 2>&1 | grep -E "UndoRedoControls|TopBar|DocumentViewer" || echo "no type errors in undo/redo files"</automated>
  </verify>
  <acceptance_criteria>
    - `UndoRedoControls.tsx` exists with two ghost buttons, canUndo/canRedo derivations, aria-disabled at limits, and per-state aria-labels.
    - Undo/redo keyboard handling lives in DocumentViewer's SINGLE keydown handler, AFTER the input guard: `grep -c "addEventListener('keydown'" src/components/DocumentViewer.tsx` returns 1 (no second listener).
    - The no-selection early return does not block undo/redo keys.
    - TopBar renders UndoRedoControls leftmost in the loaded group; tsc clean for the three files.
  </acceptance_criteria>
  <done>Undo/Redo buttons + Cmd/Ctrl+Z / Shift+Z / Ctrl+Y shortcuts work, are disabled at history bounds, and do not fire while a field input is focused.</done>
</task>

<task type="auto">
  <name>Task 2: InitialsDrawModal — draw, store PNG, arm initials placement</name>
  <files>src/components/InitialsDrawModal.tsx, src/App.tsx</files>
  <read_first>
    - src/components/SignatureDrawModal.tsx (the EXACT analog — copy its structure: signature_pad init/cleanup, focus trap, transparent PNG, controls; the initials modal mirrors it with the differences below)
    - src/App.tsx (the file being modified — SignatureDrawModal mounts unconditionally near the end; InitialsDrawModal mounts the same way)
    - src/store/fieldStore.ts (Plan 01 output — initialsModalOpen, openInitialsModal/closeInitialsModal, setInitialsDataUrl, setArmedFieldType)
    - .planning/phases/03-full-field-types-workspace-controls/03-PATTERNS.md (InitialsDrawModal section: mirror SignatureDrawModal, differences listed)
    - .planning/phases/03-full-field-types-workspace-controls/03-UI-SPEC.md (InitialsDrawModal lines 231-244; Copywriting Contract lines 340-345; canvas 2:1 ratio)
  </read_first>
  <action>
    Create `InitialsDrawModal.tsx` as a near-identical copy of `SignatureDrawModal.tsx`, gated on `initialsModalOpen` from fieldStore (via `openInitialsModal`/`closeInitialsModal` added in Plan 01) instead of `modalOpen`. Differences from the signature modal:
    - Subscribe to `initialsModalOpen`, `closeInitialsModal`, `setInitialsDataUrl`, `setArmedFieldType` (NOT modalOpen/setSignatureDataUrl).
    - Title (`aria-labelledby`): "Draw your initials" (Heading 20px/600).
    - Empty-canvas hint: "Draw here".
    - Primary CTA: "Add initials"; disabled aria-label "Add initials — draw initials first".
    - Clear button: "Clear canvas"; Discard button: "Discard" (same as signature).
    - Canvas aspect ratio 2:1 (use `padding-top: 50%` for the container instead of the signature's 33.33%).
    - On confirm (handleAddInitials): `const dataUrl = padRef.current.toDataURL('image/png')`; `setInitialsDataUrl(dataUrl)`; `setArmedFieldType('initials')`; then close. This arms the existing Plan 02 LazyPage drop path, which places a smaller (80px-default) initials image field.
    - Keep ALL of the signature modal's accessibility (role="dialog", aria-modal, focus trap, Escape closes, transparent rgba(0,0,0,0) background, devicePixelRatio scaling, `pad.off()` cleanup on unmount) verbatim.

    In `App.tsx`: mount `<InitialsDrawModal />` unconditionally alongside the existing `<SignatureDrawModal />` (it self-gates on initialsModalOpen).
  </action>
  <verify>
    <automated>cd /home/clamd/Projects/misc/free-esign && npx tsc -b 2>&1 | grep -E "InitialsDrawModal|App\\.tsx" || echo "no type errors in initials modal files"</automated>
  </verify>
  <acceptance_criteria>
    - `InitialsDrawModal.tsx` exists, gated on initialsModalOpen, with the "Draw your initials" title, "Add initials" CTA, "Draw here" hint, 2:1 canvas, and full focus-trap/cleanup parity with SignatureDrawModal.
    - On confirm it calls setInitialsDataUrl then setArmedFieldType('initials'): `grep -n "setArmedFieldType('initials')" src/components/InitialsDrawModal.tsx` matches.
    - `App.tsx` mounts InitialsDrawModal unconditionally; tsc clean.
  </acceptance_criteria>
  <done>The user can draw initials in a focus-trapped modal and, on confirm, arm initials placement that drops a smaller transparent-PNG image field via the existing path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: FLD-09 redo-tail + initials-history tests + full-suite gate</name>
  <files>src/test/undoRedo.test.ts</files>
  <read_first>
    - src/test/fieldStore.test.ts (Plan 01/02 output — undo/redo + initials state test style; reuse the makeField helper shape and getState() pattern)
    - src/store/fieldStore.ts (Plan 01 output — addField pushes history, undo/redo, FieldType, initialsDataUrl)
    - .planning/phases/03-full-field-types-workspace-controls/03-VALIDATION.md (Per-Task Verification Map FLD-09 row: "new action truncates redo tail" must be covered)
    - .planning/phases/03-full-field-types-workspace-controls/03-RESEARCH.md (Section 4: redo-tail truncation, undo/redo selection clearing)
  </read_first>
  <behavior>
    - Test (FLD-09): a new action after undo truncates the redo tail — add f1, add f2, undo (now [f1]), add f3, then redo() is a no-op and fields stays [f1, f3] (the old f2 redo path is discarded)
    - Test (FLD-09 / FLD-02): initials with history — add an 'initials' field (dataUrl set, no textValue), undo removes it, redo restores it
    - Test (FLD-09): undo/redo clears selectedFieldId
  </behavior>
  <action>
    Create a NEW store-level test file `src/test/undoRedo.test.ts` (no DOM) using `useFieldStore.getState()` with `resetFields()` in beforeEach (mirror the style in `fieldStore.test.ts`). Cover the FLD-09 redo-tail-truncation scenario explicitly (add f1 → add f2 → undo → add f3 → redo is a no-op, fields === [f1, f3]), the initials-with-history scenario (add type 'initials' with a `data:image/png;base64,...` dataUrl → undo removes → redo restores), and the selection-cleared-on-undo assertion. This file is owned solely by Plan 04 (no cross-plan ownership conflict). This task is also the phase test-gate — the verify command runs the FULL suite.
  </action>
  <verify>
    <automated>cd /home/clamd/Projects/misc/free-esign && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `src/test/undoRedo.test.ts` exists and is green: redo-tail truncation, initials undo/redo, selection cleared on undo.
    - Full `npx vitest run` suite is GREEN (fieldStore, exportPdf, fileValidation, coordinateMapper, documentStore, fieldPlacement, undoRedo, plus all pre-existing Phase 1/2 suites).
  </acceptance_criteria>
  <done>FLD-09 redo-tail truncation and initials-history integration are covered in a dedicated test file, and the complete suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| global keydown → undo/redo | Keyboard events must not hijack typing in field inputs |
| initials canvas → transparent PNG | Drawn strokes become an embeddable image data URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Tampering | undo/redo keydown vs input focus | mitigate | The DocumentViewer keydown guard returns early for INPUT/TEXTAREA/contentEditable BEFORE any undo/redo branch, so shortcuts never fire while typing (RESEARCH Section 4, Pitfall 8) |
| T-03-12 | DoS | InitialsDrawModal signature_pad listeners | mitigate | `pad.off()` on unmount (mirrors SignatureDrawModal T-02-06); fixed modal max-width caps canvas size |
| T-03-13 | Information Disclosure | initialsDataUrl | mitigate | In-memory only this phase (persistence is Phase 4); cleared by resetFields on new-document load |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — RESEARCH Package Legitimacy Audit: all deps pre-verified; no install tasks |
</threat_model>

<verification>
- `npx vitest run` full suite green.
- `npx tsc -b` clean across the project.
- `grep -c "addEventListener('keydown'" src/components/DocumentViewer.tsx` returns 1 (single keydown handler hosts both delete and undo/redo).
- Manual-only (end-of-phase human verification per 03-VALIDATION.md): Cmd/Ctrl+Z inside a focused text field edits text (does not undo placement); initials render and export correctly.
</verification>

<success_criteria>
- Undo/Redo buttons + Cmd/Ctrl+Z / Shift+Z / Ctrl+Y work, disabled at history bounds, inert while a field input is focused.
- InitialsDrawModal draws initials and arms 'initials' placement producing a smaller transparent-PNG field.
- App mounts InitialsDrawModal; whole-project tsc clean.
- Full test suite green; FLD-09 redo-tail truncation + initials-history covered in undoRedo.test.ts.
</success_criteria>

<output>
Create `.planning/phases/03-full-field-types-workspace-controls/03-04-SUMMARY.md` when done.
</output>
