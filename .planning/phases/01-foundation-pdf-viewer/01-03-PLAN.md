---
phase: 01-foundation-pdf-viewer
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/lib/fileValidation.ts
  - src/lib/imageWrapper.ts
  - src/components/UploadZone.tsx
  - src/components/ErrorBanner.tsx
  - src/App.tsx
  - src/store/documentStore.ts
  - src/test/fileValidation.test.ts
  - src/test/imageWrapper.test.ts
autonomous: true
requirements: [DOC-01, DOC-02]
user_setup: []

must_haves:
  truths:
    - "A user can drag a PDF anywhere onto the empty state, or click Browse and pick one, and it opens"
    - "A user can open a JPG or PNG; it is wrapped into a single-page PDF and displayed as a document"
    - "Picking an unsupported type, a file over 100MB, or a corrupt file shows a friendly inline error with a 'Try another file' retry — never a raw browser error or crash"
    - "Once a document is loaded, an 'Open another' control switches documents without a page reload"
  artifacts:
    - path: "src/lib/fileValidation.ts"
      provides: "Pure type+size validation: accept pdf/jpeg/png by MIME and extension; reject >100MB"
      exports: ["validateFile"]
    - path: "src/lib/imageWrapper.ts"
      provides: "pdf-lib JPG/PNG → single-page Blob-URL PDF sized to the image"
      exports: ["wrapImageAsPdf"]
    - path: "src/components/UploadZone.tsx"
      provides: "Full-screen drag-drop + Browse empty state per UI-SPEC"
      contains: "Browse files"
    - path: "src/components/ErrorBanner.tsx"
      provides: "Inline role=alert error card with dynamic copy + retry"
      contains: "Try another file"
  key_links:
    - from: "src/components/UploadZone.tsx"
      to: "src/lib/fileValidation.ts + src/lib/imageWrapper.ts + documentStore"
      via: "on file select: validate → (image ? wrapImageAsPdf : createObjectURL) → loadDocument / setError"
      pattern: "(validateFile|wrapImageAsPdf|loadDocument|setError)"
---

<objective>
Deliver the full document-ingestion slice: a full-screen drag-and-drop + Browse empty state, type/size validation, image→PDF wrapping on load, friendly inline error + retry, and an "Open another" control. This replaces the skeleton's temporary file input with the real upload UX and adds image support, so the user can open a PDF, a JPG, or a PNG — covering DOC-01 and DOC-02.

Purpose: Make opening a document real, safe, and uniform — every input becomes a PDF in the viewer (the "everything is a PDF" pipeline that Phase 2 overlays/exports). Validation here is also the primary security control (file ingestion is the main attack surface).
Output: UploadZone, ErrorBanner, fileValidation + imageWrapper utilities (with unit tests), and the App wiring that routes a selected file into the existing view state machine.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@CLAUDE.md
@.planning/phases/01-foundation-pdf-viewer/01-RESEARCH.md
@.planning/phases/01-foundation-pdf-viewer/01-UI-SPEC.md

<interfaces>
<!-- Consumed from Plan 01 (already built). Use directly — no exploration. -->
From src/store/documentStore.ts:
```typescript
useDocumentStore: {
  view: 'empty'|'loading'|'error'|'loaded'
  loadDocument: (url: string) => void   // sets docUrl, view='loading'
  setError: (msg: string) => void       // sets errorMessage, view='error'
  reset: () => void                     // back to empty
  errorMessage: string | null
}
```
App.tsx (Plan 01) already renders a view-router: replace its temporary `empty` branch (the placeholder file input) with <UploadZone/>, and its `error` branch with <ErrorBanner/>.

<!-- Contract this plan ESTABLISHES -->
From src/lib/fileValidation.ts:
```typescript
export type FileValidationError = 'unsupported-type' | 'too-large' | null
export function validateFile(file: File): FileValidationError   // null = valid
```
From src/lib/imageWrapper.ts:
```typescript
export function wrapImageAsPdf(file: File): Promise<string>   // returns a Blob URL of a 1-page PDF
```
</interfaces>

<copy_contract>
<!-- From UI-SPEC Copywriting Contract — use these strings verbatim. -->
- Empty heading: "Drop your document here"
- Browse CTA: "Browse files"
- Secondary prompt: "or"
- Privacy line: "Your files never leave your browser."
- Error heading: "Could not open file"
- Error (corrupt/unreadable/password-protected): "This file couldn't be read. It may be corrupt or password-protected. Try another file."
- Error (wrong type): "Only PDF, JPG, and PNG files are supported. Try another file."
- Error (>100MB): "This file is too large to open in the browser. Try a smaller file."
- Retry action: "Try another file"
- Top-bar action (loaded): "Open another"
</copy_contract>
</context>

## Phase Goal

**As a** person who needs to sign a document, **I want to** open a PDF or image and see every page rendered in my browser, **so that** I can review it knowing the file never leaves my device.

(This plan delivers the open-a-document slice: drag-drop/browse a PDF or image, with safe validation and friendly errors.)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: File validation + image→PDF wrapping utilities (with unit tests)</name>
  <files>src/lib/fileValidation.ts, src/lib/imageWrapper.ts, src/test/fileValidation.test.ts, src/test/imageWrapper.test.ts</files>
  <read_first>
    - 01-RESEARCH.md "Pattern 4: Image → PDF Wrapping", "Verified: pdf-lib image wrap", "Security Domain" (V5 input validation, 100MB limit, try/catch around wrapping), Pitfall 4 (DPI note — document for Phase 2, do not change behavior)
    - 01-UI-SPEC.md "Self-Hosted Asset Requirements" + Copywriting Contract (error mapping)
    - The <interfaces> block (exact signatures) and <copy_contract>
  </read_first>
  <behavior>
    - validateFile: a `.pdf` (application/pdf) → null; `.jpg/.jpeg` (image/jpeg) → null; `.png` (image/png) → null.
    - validateFile: a `.txt`/`.docx`/unknown MIME or mismatched extension → 'unsupported-type'.
    - validateFile: any allowed type but size > 100 * 1024 * 1024 → 'too-large' (checked before reading bytes).
    - wrapImageAsPdf: a small valid PNG File → resolves to a string starting with `blob:`.
    - wrapImageAsPdf: a small valid JPEG File → resolves to a `blob:` URL.
  </behavior>
  <action>
    Write the unit tests FIRST (src/test/fileValidation.test.ts, src/test/imageWrapper.test.ts) covering the behaviors above, then implement to GREEN.

    src/lib/fileValidation.ts — pure `validateFile(file)` returning FileValidationError. Whitelist by BOTH MIME type and file extension (defense in depth, RESEARCH V5): allowed MIME {application/pdf, image/jpeg, image/png}, allowed extensions {.pdf,.jpg,.jpeg,.png}. Enforce the 100MB size cap BEFORE any byte read (RESEARCH DoS mitigation). Return 'too-large' / 'unsupported-type' / null. Keep it pure (no DOM).

    src/lib/imageWrapper.ts — `wrapImageAsPdf(file)` per RESEARCH Pattern 4: `PDFDocument.create()`, embedPng for image/png else embedJpg, `addPage([image.width, image.height])`, `drawImage` at {x:0,y:0,width,height}, `save()`, return `URL.createObjectURL(new Blob([bytes],{type:'application/pdf'}))`. Embed original bytes only — never canvas-rasterize (CLAUDE.md document-integrity; embedJpg/embedPng keep original bytes). Wrap the body in try/catch and rethrow a tagged error the caller maps to the corrupt-file copy. Add a brief code comment recording the DPI caveat (RESEARCH Pitfall 4) for Phase 2 export.

    For imageWrapper tests in jsdom: construct tiny valid PNG/JPEG byte fixtures (a 1×1 PNG/JPEG is sufficient) as File objects; assert the returned string starts with `blob:`. If jsdom lacks `URL.createObjectURL`, stub it in the test (or in src/test/setup.ts) to return a `blob:` string so the assertion is meaningful.
  </action>
  <verify>
    <automated>npx vitest run src/test/fileValidation.test.ts src/test/imageWrapper.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - validateFile accepts pdf/jpeg/png (MIME + extension) and rejects others with 'unsupported-type'; rejects >100MB with 'too-large' before reading bytes.
    - wrapImageAsPdf returns a `blob:` URL for valid PNG and JPEG inputs and uses embedPng/embedJpg (no canvas rasterization).
    - Both test files are GREEN.
  </acceptance_criteria>
  <done>The ingestion logic (validate + image-wrap) is implemented, pure where possible, and unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: UploadZone + ErrorBanner, wired into the App view-router</name>
  <files>src/components/UploadZone.tsx, src/components/ErrorBanner.tsx, src/App.tsx, src/store/documentStore.ts</files>
  <read_first>
    - 01-UI-SPEC.md "Component Inventory" (UploadZone, ErrorBanner exact dims/colors/copy), "Interaction States" (drag-over, hover/focus/active), "Accessibility Requirements" (role=region/aria-label on zone, hidden file input, role=alert on error, 44px targets, focus rings)
    - 01-RESEARCH.md "Security Domain" (try/catch → friendly error)
    - The <interfaces> block (store actions, App branches to replace) and <copy_contract>
  </read_first>
  <action>
    Build src/components/UploadZone.tsx per UI-SPEC: full-screen zone (min-height calc(100dvh - 56px)), centered 480px content column, 48px upload SVG, heading "Drop your document here", "or", an accent "Browse files" button (min 44px) that activates a visually-hidden `<input type="file" accept=".pdf,.jpg,.jpeg,.png">`, and the privacy line "Your files never leave your browser." Implement HTML5 drag events on the whole zone (dragenter/dragover/dragleave/drop) with the drag-over visual state (2px dashed accent border + faint white tint). `role="region"`, `aria-label="Document upload area"`, visible focus rings.

    Shared handler `handleFile(file)`: call `validateFile(file)`; on 'unsupported-type' → `setError("Only PDF, JPG, and PNG files are supported. Try another file.")`; on 'too-large' → `setError("This file is too large to open in the browser. Try a smaller file.")`. On valid: if image/jpeg or image/png → `await wrapImageAsPdf(file)` inside try/catch (catch → setError corrupt-file copy), else `URL.createObjectURL(file)`; then `loadDocument(url)`. (Set view='loading' via loadDocument; the existing viewer/onLoadError path will surface render-time corruption — see store note below.) Use the same handler for both drop and picker.

    Build src/components/ErrorBanner.tsx per UI-SPEC: inline card (not modal), 4px destructive left border, white bg, 16px red warning SVG, heading "Could not open file", body = store `errorMessage`, and a "Try another file" action that calls `reset()`. `role="alert"`.

    Wire src/App.tsx: replace the Plan-01 temporary `empty` branch with <UploadZone/> and the `error` branch with <ErrorBanner/> (keep LoadingSpinner for 'loading' and DocumentViewer for 'loaded'). Ensure the TopBar "Open another" control (loaded state) revokes the old Blob URL and calls reset() so the user returns to the empty state without a reload. Add a small store action or helper if needed to revoke `docUrl` before reset (extend documentStore.ts minimally; do not break the Plan-01 contract). Also add a render-failure path: pass an `onLoadError`/`onSourceError` handler (set in DocumentViewer or via the store) that calls `setError("This file couldn't be read. It may be corrupt or password-protected. Try another file.")` so a corrupt PDF that passes validation still lands in the friendly error state rather than a blank canvas.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -6 && npx tsc --noEmit 2>&1 | tail -5 && echo WIRED_OK</automated>
  </verify>
  <acceptance_criteria>
    - UploadZone renders the exact UI-SPEC copy, accepts drop anywhere on the zone and via Browse, filters the picker to .pdf/.jpg/.jpeg/.png, and shows the drag-over state.
    - Invalid type and >100MB show the correct ErrorBanner copy; the retry returns to the empty state.
    - Opening a PNG/JPG wraps it to a PDF Blob URL and transitions to the viewer; opening a PDF does too.
    - "Open another" revokes the prior Blob URL and resets to empty with no page reload.
    - tsc --noEmit passes; full `npx vitest run` is green.
  </acceptance_criteria>
  <done>The real upload UX (drag-drop + browse), image support, validation, friendly errors, and "Open another" are live and wired into the existing state machine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user file → app | The dropped/picked file is untrusted input; this is Phase 1's primary attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | DoS | oversized file → OOM/hang | mitigate | validateFile enforces a 100MB cap BEFORE `file.arrayBuffer()` is ever called (RESEARCH DoS). |
| T-01-06 | Tampering | unsupported/spoofed file type → pdf-lib/pdfjs | mitigate | Whitelist by MIME AND extension in validateFile; only allowed types reach wrapImageAsPdf / the viewer. |
| T-01-07 | Tampering | malicious/corrupt PNG/JPG throws in pdf-lib | mitigate | wrapImageAsPdf wrapped in try/catch → friendly ErrorBanner (no crash, no raw browser error). |
| T-01-08 | Tampering | corrupt PDF passes validation, fails at render | mitigate | DocumentViewer onLoadError/onSourceError → setError(corrupt-file copy) → ErrorBanner instead of blank canvas. |
| T-01-SC | Tampering | npm installs | n/a | No new packages (pdf-lib + zustand already installed by Plan 01; audit cleared in RESEARCH). |
</threat_model>

<verification>
- `npx vitest run` green (adds fileValidation + imageWrapper suites).
- `npx tsc --noEmit` passes.
- Manual: drag a PDF onto the zone and Browse a PNG — both open; a .txt and a >100MB file each show the correct error and retry.
</verification>

<success_criteria>
- DOC-01: a user opens a PDF by drag-and-drop or file picker.
- DOC-02: a user opens a JPG or PNG, displayed as a document (wrapped to PDF internally).
- All ingestion errors are friendly, inline, and recoverable — no crashes or raw browser errors.
- "Open another" switches documents without a reload, revoking the prior Blob URL.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-pdf-viewer/01-03-SUMMARY.md` when done.
</output>
