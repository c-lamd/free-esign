# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules/
dist/
dist-ssr/
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# TypeScript build info
*.tsbuildinfo

# GSD planning + agent tooling — kept local; not part of the open-source repo.
# (Internal process docs and scratch; the curated design lives in docs/design/.)
.planning/
.claude/

# Self-hosted pdf.js assets — generated into public/ by scripts/copy-pdf-assets.mjs
# (prepare/predev/prebuild). Never commit; they must stay in sync with the installed
# pdfjs-dist version and would otherwise bloat the repo.
public/pdf.worker.min.mjs
public/cmaps/
public/standard_fonts/
